import React from "react";
import { EditorView } from "codemirror";

const Editor = () => {
  return(
      <textarea id="editerPort" className="h-full w-full p-2 text-white"></textarea>
  );
};

export default Editor;
